{% extends 'base.html.twig' %}

{% block title %}Emprunt{% endblock %}

{% block body %}
    <h1>Emprunt</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ loan.id }}</td>
            </tr>
            <tr>
                <th>Emprunteur</th>
                <td>{{ loan.user.firstName }} {{loan.user.lastName }}</td>
            </tr>
            <tr>
                <th>Date de début</th>
                <td>{{ loan.createdAt ? loan.createdAt|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Date de fin</th>
                <td>{{ loan.finishedAt ? loan.finishedAt|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Date de retour</th>
                <td>{{ loan.returnedAt ? loan.returnedAt|date('Y-m-d') : 'Pas encore rendu' }}</td>
            </tr>
            <tr>
                <th>Objet</th>
                <td>{{ loan.ressource.label }}</td>
            </tr>
        </tbody>
    </table>

    <a class="button hollow primary" href="{{ path('loan_index') }}">Retour</a>

    <a class="button hollow primary" href="{{ path('loan_edit', {'id': loan.id}) }}">Modifier</a>

    {{ include('loan/_delete_form.html.twig') }}
{% endblock %}
