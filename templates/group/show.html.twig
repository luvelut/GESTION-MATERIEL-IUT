{% extends 'base.html.twig' %}

{% block title %}Groupe{% endblock %}

{% block body %}
    <h1>Groupe</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ group.id }}</td>
            </tr>
            <tr>
                <th>Label</th>
                <td>{{ group.label }}</td>
            </tr>
        </tbody>
    </table>

    <a class="button hollow primary" href="{{ path('group_index') }}">Retour</a>

    <a class="button hollow primary" href="{{ path('group_edit', {'id': group.id}) }}">Modifier</a>

    {{ include('group/_delete_form.html.twig') }}
{% endblock %}
