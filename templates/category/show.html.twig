{% extends 'base.html.twig' %}

{% block title %}Categorie{% endblock %}

{% block body %}
    <h1>{{ category.label }}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category.id }}</td>
            </tr>
            <tr>
                <th>Label</th>
                <td>{{ category.label }}</td>
            </tr>
        </tbody>
    </table>

    <a class="hollow button primary" href="{{ path('category_index') }}">Retour</a>
    <a class="hollow button primary" href="{{ path('category_edit', {'id': category.id}) }}">Modifier</a>


    {{ include('category/_delete_form.html.twig') }}
{% endblock %}
